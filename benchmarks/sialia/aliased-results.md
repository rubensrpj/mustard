# Wave 2b - PT->EN equivalence query expansion vs the dict-seeded PageRank baseline

Generated by `benchmarks/sialia/pagerank-aliased.ps1` on 2026-07-08 14:41.
Retrieval under test: the SAME `grain rank` ranker, on an EXPANDED query = raw PT intent + the English equivalents of each domain token (`equivalences.json`, 214 folded-PT keys). Régua identical to the baselines: scored labels (n=13), target-OR-secondary within top-K.

## Aggregate (scored labels only, n=13)

| Retrieval | Acc@5 | Acc@10 | hit ids @5 |
|---|---|---|---|
| raw-PT (default) | 6/13 (46,2%) | 6/13 (46,2%) | 1,3,4,5,6,12 |
| raw-PT + equiv (default) | 5/13 (38,5%) | 6/13 (46,2%) | 1,4,5,6,12 |
| raw-PT + equiv (idf seed) | 6/13 (46,2%) | 8/13 (61,5%) | 1,3,4,5,6,12 |
| raw-PT (idf seed) | 4/13 (30,8%) | 5/13 (38,5%) | 3,4,5,8 |
| — digest baseline raw-PT | 2/13 (15.4%) | 2/13 (15.4%) | 2,4 |
| — digest baseline PT+EN  | 6/13 (46.2%) | 6/13 (46.2%) | 2,4,7,8,9,14 |

**Headline (default config, same régua as 46.2%): raw-PT 6/13 (46,2%) -> raw-PT+equiv 5/13 (38,5%).**

## Movers (expansion vs raw, default config)

- Gained (expand hit\@5, raw miss): (none)
- Lost   (raw hit\@5, expand miss): id 3
- Gained ONLY under idf seeding (non-default): (none)

## Per-prompt (default config)

| id | diff | raw hit@5 | +equiv hit@5 | +equiv(idf) hit@5 | target rank (+equiv) | added EN tokens |
|---:|---|:---:|:---:|:---:|:---:|---|
| 1 | media | Y | Y | Y | miss | field notes form contract |
| 2 | facil | . | . | . | miss | chart accounts payable pay duedate due maturity aging bucket band bar days |
| 3 | media | Y | . | Y | 6 | tab documents registration register partner |
| 4 | facil | Y | Y | Y | miss | card status sync contract |
| 5 | facil | Y | Y | Y | 2 | validation validate cnpj document schema zod partner |
| 6 | facil | Y | Y | Y | miss | field type response contract |
| 7 | facil | . | . | . | miss | hook list channels saleschannels sales sale |
| 8 | media | . | . | . | miss | value amount status approval approved bank |
| 9 | dificil | . | . | . | miss | split breakdown accounts payable pay |
| 10 | na | . | . | . | miss | type commission |
| 11 | dificil | . | . | . | miss | calculation calculate date duedate due maturity installment parcel contract |
| 12 | media | Y | Y | Y | 4 | validation validate handler service creation create contract |
| 13 | dificil | . | . | . | miss | reconciliation reconcile statement bank |
| 14 | dificil | . | . | . | miss | field configuration configure entity invoice receivable bill title payable financialtitle |
| 15 | dificil | . | . | . | miss | reseller dashboard referrals plans salesplans list channel saleschannel sales sale partner clients customers registration register client customer earnings contract |

Note: id 10 & id 15 are `scored:false` (excluded from the aggregate).

## Target-rank probe — the English-concept misses (rank of target/secondary in top-50)

| target | raw (default) | +EN equiv (default) | +EN equiv (idf seed) |
|---|:---:|:---:|:---:|
| id7 use-sales-channels.ts (primary, no secondary) | miss>50 | miss>50 | miss>50 |
| id8 BankApprovalStatus.cs (primary) | miss>50 | miss>50 | miss>50 |
| id8 banks/banks.zod.ts (secondary) | 30 | 36 | 9 |
| id14 ReceivableConfiguration.cs (primary) | miss>50 | miss>50 | miss>50 |
| id14 PayableConfiguration.cs (secondary) | miss>50 | miss>50 | miss>50 |
| id3 partners/.../form/form-context.tsx (raw hit) | 4 | 6 | 2 |

Under the default (specificity) config the English tokens are **inert-to-harmful**: they never promote a miss target into the top-5, and the broad-term equivalents (`valor→amount`, `status`) actually DEMOTE the one reachable secondary (id8 `banks.zod.ts` 30 → 36). The single positive movement (→9) comes from switching the SEED WEIGHT to idf, which moves the RAW query too — so no id7/id8/id14 hit is attributable to translation.

## Diagnosis (honest) — the bridge does NOT lift 46.2%; under the default config it regresses to 38.5%

The equivalences are linguistically correct (`canal→channel`, `aprovação→approved`, `configuração→configure`), but the ranker's architecture defeats them for these targets — three compounding STRUCTURAL causes, not a bad dictionary:

1. **Dict-gated seeding makes most equivalents inert.** `grain rank` seeds a query token only if it matches a *distinctive-dictionary term* (`pagerank.rs` MATCH, lines 351-380), which then seeds modules whose identifiers fold-equal it (`token_seeds`) ∪ its dict anchors. The English identifiers of the miss targets — `sales`, `channel`, `bank`, `receivable`, `configuration`, `status`, `entity`, `invoice` — are NOT dictionary terms (only PT `venda`/`canal`/`entidade`/`configuração` and code words `approved`/`configure`/`duedate`/`installment`/`supplier` made the distinctive top-500). Every equivalent that isn't a dict term adds exactly zero seed mass.
2. **The few equivalents that ARE dict terms anchor the wrong (hub) cluster.** `approved` (df 28) anchors contract files; `configure` (df 117) anchors DI bootstrap; `amount` (from `valor`) anchors dashboards. They reach the real target only through one thin `token_seeds` edge (enum member `APPROVED`; method `Configure`) that is drowned by their high-frequency anchors — pouring mass into contracts/DI/dashboards and DEMOTING borderline raw hits (id3: 4 → 6, the entire −7.7pp regression).
3. **The miss targets are peripheral nodes the fan-in penalty pushes down.** `use-sales-channels.ts` has ~0 in-model importers, `BankApprovalStatus.cs` fan_in 9 (demoted by the 1.0 fan-in penalty), `ReceivableConfiguration.cs` fan_in 1 (leaf). Personalized PageRank lifts import-CENTRAL files; these are the opposite. Same disconnected-graph failure already diagnosed for id9/id13 (a PT-anchored frontend seed can't reach a peripheral C# backend node) — id14's backend EF config is exactly that case.

**Verdict.** Query expansion via `equivalences.json` does not recover id7/id8/id14 at Acc@5 and, under the shipped default config, LOWERS the score (46.2% → 38.5%). The 61.5% Acc@10 line is real but comes from the idf seed weight, not the translation. To cash the PT→EN bridge for these English-identifier targets the RANKER must change: seed `token_seeds` DIRECTLY from the (expanded) query tokens, ungated by dict membership — letting `sales`/`channel`/`configuration`/`bank` seed the files whose identifiers contain them. The bilingual dictionary is a necessary input for that fix but is inert on its own while the seed path stays dict-gated. (The digest baseline wins id7/8/14 precisely because it matches the English gloss against identifiers directly, with no dictionary gate.)


