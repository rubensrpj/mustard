# Wave 2b - UNGATED ranker fix + PT->EN equivalence expansion vs the 46.2% baseline

Generated by `benchmarks/sialia/pagerank-aliased.ps1` on 2026-07-08 15:31.
The fix (`apps/scan/src/pagerank.rs`): query tokens seed module IDENTIFIERS DIRECTLY, ungated by dictionary membership (the English-gloss baseline's move), plus a fan-in-EXEMPT base floor so a directly-named low-centrality target (EF config, enum) ranks on its own seed. Query = raw PT intent + the English equivalents of each domain token (`equivalences.json`, 214 folded-PT keys). Régua identical to the baselines: scored labels (n=13), target-OR-secondary within top-K. The pre-fix rows use `--no-direct-seed` (dict-gated); the fix rows are the ranker default.

## Aggregate (scored labels only, n=13)

| Retrieval | Acc@5 | Acc@10 | hit ids @5 |
|---|---|---|---|
| dict-gated raw-PT (pre-fix) | 6/13 (46,2%) | 6/13 (46,2%) | 1,3,4,5,6,12 |
| dict-gated raw+equiv (pre-fix) | 5/13 (38,5%) | 6/13 (46,2%) | 1,4,5,6,12 |
| UNGATED raw-PT (fix, no equiv) | 5/13 (38,5%) | 7/13 (53,8%) | 1,4,5,6,12 |
| UNGATED raw+equiv (fix) | 6/13 (46,2%) | 7/13 (53,8%) | 1,3,4,5,6,12 |
| — digest baseline raw-PT | 2/13 (15.4%) | 2/13 (15.4%) | 2,4 |
| — digest baseline PT+EN  | 6/13 (46.2%) | 6/13 (46.2%) | 2,4,7,8,9,14 |

**Headline (same régua as 46.2%): pre-fix dict-gated raw-PT 6/13 (46,2%) -> UNGATED raw+equiv (fix) 6/13 (46,2%) Acc@5, 7/13 (53,8%) Acc@10.**

## Movers (UNGATED raw+equiv fix vs pre-fix dict-gated raw-PT)

- Gained (fix hit\@5, pre-fix miss): (none)
- Lost   (pre-fix hit\@5, fix miss): (none)
- Attributable to the EQUIVALENCES (fix+equiv hit\@5, fix-no-equiv miss): id 3

## Per-prompt

| id | diff | pre-fix hit@5 | fix(no-equiv) hit@5 | fix+equiv hit@5 | target rank (fix+equiv) | added EN tokens |
|---:|---|:---:|:---:|:---:|:---:|---|
| 1 | media | Y | Y | Y | miss | field notes form contract |
| 2 | facil | . | . | . | 8 | chart accounts payable pay duedate due maturity aging bucket band bar days |
| 3 | media | Y | . | Y | 5 | tab documents registration register partner |
| 4 | facil | Y | Y | Y | miss | card status sync contract |
| 5 | facil | Y | Y | Y | 3 | validation validate cnpj document schema zod partner |
| 6 | facil | Y | Y | Y | miss | field type response contract |
| 7 | facil | . | . | . | miss | hook list channel channels saleschannel saleschannels sales sale |
| 8 | media | . | . | . | miss | value amount status approval approved bank |
| 9 | dificil | . | . | . | miss | split breakdown accounts payable pay |
| 10 | na | . | . | . | miss | type commission |
| 11 | dificil | . | . | . | miss | calculation calculate date duedate due maturity installment parcel contract |
| 12 | media | Y | Y | Y | 5 | validation validate handler service creation create contract |
| 13 | dificil | . | . | . | miss | reconciliation reconcile statement bank |
| 14 | dificil | . | . | . | miss | field configuration configure entity invoice receivable bill title payable financialtitle |
| 15 | dificil | . | . | . | miss | reseller dashboard referrals plans salesplans list channel channels saleschannel sales sale partner clients customers registration register client customer earnings contract |

Note: id 10 & id 15 are `scored:false` (excluded from the aggregate).

## Target-rank probe — the English-concept misses (rank of target/secondary in top-50)

| target | pre-fix raw (dict-gated) | pre-fix +EN (dict-gated) | FIX +EN (ungated) |
|---|:---:|:---:|:---:|
| id7 use-sales-channels.ts (primary, no secondary) | miss>50 | miss>50 | 29 |
| id8 BankApprovalStatus.cs (primary) | miss>50 | miss>50 | 19 |
| id8 banks/banks.zod.ts (secondary) | 30 | 36 | 32 |
| id14 ReceivableConfiguration.cs (primary) | miss>50 | miss>50 | miss>50 |
| id14 PayableConfiguration.cs (secondary) | miss>50 | miss>50 | miss>50 |
| id3 partners/.../form/form-context.tsx (raw hit) | 4 | 6 | 5 |


## Diagnosis (honest) — the ungate fixes the SEEDING (Acc@10 46.2%->53.8%, targets now reachable) but does NOT close id7/8/14 at Acc@5

**What the fix does (measured, base 100000):**
- **Acc@5 HELD at 46.2%** (no regression): the ungate alone drops id3, but the PT->EN equivalences recover it (`Attributable to the EQUIVALENCES` = id3), netting zero @5 change.
- **Acc@10 46.2% -> 53.8%**: id2 (`aging-bar.tsx`) reaches rank 8 — pre-fix it was unreachable. The direct `aging` identifier match now floors it into the top-10.
- **The English-identifier targets go from UNREACHABLE to reachable**: dict-gated pre-fix could not seed them at all (miss>50); the ungate seeds them — id8 `BankApprovalStatus.cs` -> rank ~19, id7 `use-sales-channels.ts` -> ~29. The seeding WAS the blocker, as Wave-2b predicted.

**Why id7/8/14 still miss Acc@5 (the honest limit):** the ungate proves seeding was the blocker, but a single GLOBAL floor cannot rank them top-5 without displacing the dict-route hits, because the English-identifier targets are NOT the uniquely-strongest identifier match — they share their domain words with many siblings:
- **id7**: `channel`/`sales` occur in ~117 files (every sales-channels page/route/loading/backend + the hook); the target hook is one of 117.
- **id8**: `bank`+`approval`+`status` also match `IBankApprovalService`, `BankApprovalService`, `PartnerApprovalService`, and every `*Status` enum.
- **id14**: `configuration`/`configure` is a 40-way-common EF pattern (every `IEntityTypeConfiguration.Configure`); `receivable` matches the whole receivable cluster (DTOs/services/repos/zod/entity). The target is not discriminable from its ~40 sibling configs — the same STRUCTURAL class as id9/id13, not a translation gap.

Raising the floor weight to force these into top-5 (base >=150k) promotes sibling noise and drops id3/5/12 (`partners.zod` 3->6, displaced by `partners-import.zod`/`DocumentValidator`). base 100000 is the strongest floor that preserves 46.2% Acc@5.

**id9/id13 remain graph-disconnected** (confirmed unchanged): the PT comment-term seed (`desdobramento`, `extrato`) anchors the frontend, and no import edge carries mass to the peripheral C# backend service — a disconnected cross-language component, exactly as diagnosed pre-fix. id14 joins this class (target undiscriminable from siblings).

**What would close @5 (follow-up, not a config knob):** the English-gloss digest baseline wins id7/8/14 because its BM25-over-identifiers adds declaration-KIND weighting (a top-level `const` hook / `enum` / `class` outranks incidental member matches) on top of idf+length. Porting that kind-weight into the direct-match floor is the remaining step; raw idf-sum seeding gets the targets reachable and lifts Acc@10, but not into top-5 against their domain siblings.


