'use strict';

// Catalog of every /mustard:* slash command. Surfaced by the dashboard "Comandos" tab.
// Each entry has BOTH a plain-language explanation (simples) and a technical one (tecnico)
// so non-developers and developers can find the same command useful.

const COMMANDS = [
  // ── Pipeline ──────────────────────────────────────────────────────
  {
    cmd: '/mustard:feature',
    syntax: '/mustard:feature [slug]',
    category: 'Pipeline',
    short: 'Inicia o pipeline para uma nova feature ou enhancement.',
    simples: 'Use quando quiser que o Claude implemente uma nova funcionalidade do começo ao fim. Você descreve o que quer (ou cola um PRD), e ele pesquisa o código, escreve uma spec, pede sua aprovação, implementa, testa, e fecha quando tudo estiver verde.',
    tecnico: 'Dispara as fases ANALYZE → PLAN → /approve → EXECUTE → QA → CLOSE. Em scope=light pula o PLAN. Cria .claude/spec/active/<date>-<slug>/spec.md com seções Boundaries/Checklist/Acceptance Criteria/Decisões/Non-Goals. Despacha agentes (Explore, Plan, general-purpose) com budgets por role e roteamento de modelo gated. close-gate exige build/lint/test/QA antes de mover para completed/.',
    when: 'Implementar funcionalidade nova que toca produção (schema, API, UI). Specs envolvendo 3+ camadas ou nova entidade.',
    notWhen: 'Bug isolado → use bugfix. Spike/exploração sem produção → use task. Apenas análise → use task ou scan.',
    examples: ['/mustard:feature add-user-auth', '/mustard:feature  (cole o PRD logo abaixo)'],
    seeAlso: ['approve', 'resume', 'complete', 'qa'],
  },
  {
    cmd: '/mustard:bugfix',
    syntax: '/mustard:bugfix [slug]',
    category: 'Pipeline',
    short: 'Pipeline focado em correção de defeitos.',
    simples: 'Use quando algo está quebrado e você precisa consertar. O Mustard reproduz o bug, isola o código culpado, escreve um teste que falha, corrige, e só fecha quando o teste passa.',
    tecnico: 'Pipeline com Fast Path próprio (light por default). Fases: ANALYZE (reproduce + isolate) → EXECUTE (test-first fix) → QA → CLOSE. Difere do feature por pular PLAN/approve quando escopo é claro e por exigir reprodução documentada na spec.',
    when: 'Erro reproduzível, regressão, comportamento incorreto.',
    notWhen: 'Refactor sem bug → use feature com escopo light. Mudança de design → use feature.',
    examples: ['/mustard:bugfix login-redirect-loop'],
    seeAlso: ['feature', 'qa', 'resume'],
  },
  {
    cmd: '/mustard:approve',
    syntax: '/mustard:approve',
    category: 'Pipeline',
    short: 'Aprova a spec atual e libera o EXECUTE.',
    simples: 'No meio do pipeline, o Mustard escreve a spec e pede aprovação antes de implementar. Use este comando para dizer "sim, pode mexer no código agora".',
    tecnico: 'Em scope=full, marca a transição PLAN → EXECUTE no pipeline-state. Sem isso, o pipeline fica esperando. Em scope=light a aprovação é implícita (sem PLAN explícito).',
    when: 'Após revisar a spec gerada e estar de acordo com Boundaries + AC + Checklist.',
    notWhen: 'Se a spec ainda precisa de ajustes — peça mudanças antes de aprovar.',
    examples: ['/mustard:approve'],
    seeAlso: ['feature', 'resume'],
  },
  {
    cmd: '/mustard:complete',
    syntax: '/mustard:complete [spec-name]',
    category: 'Pipeline',
    short: 'Finaliza o pipeline ativo (executa CLOSE).',
    simples: 'Marca a spec como concluída quando todo o trabalho está pronto. Move a pasta de active/ para completed/ e arquiva métricas.',
    tecnico: 'Roda close-gate (build + lint + test + QA + checklist 100%). Se tudo passa, move .claude/spec/active/<spec>/ para .claude/spec/completed/<spec>/, arquiva metrics em .claude/metrics/, e atualiza pipeline-state. Bloqueia se MUSTARD_CLOSE_GATE_MODE=strict e algum check falha.',
    when: 'Spec entregue e validada. Útil para fechar specs órfãs detectadas em /mustard:status.',
    notWhen: 'Build broken, testes falhando ou checklist incompleto.',
    examples: ['/mustard:complete', '/mustard:complete 2026-04-21-retry-context-fix'],
    seeAlso: ['qa', 'maint', 'status'],
  },
  {
    cmd: '/mustard:resume',
    syntax: '/mustard:resume [spec-name]',
    category: 'Pipeline',
    short: 'Retoma um pipeline interrompido ou em erro.',
    simples: 'Se algo deu errado no meio do pipeline (ex: API caiu, contexto explodiu), use este comando para o Mustard continuar de onde parou.',
    tecnico: 'Lê pipeline-state.lastDispatchFailure e retoma a fase que estava em curso. Auto-recupera dispatch failures (HTTP 5xx, overload, tool result missing) dentro de 10 min. Reusa prior agent memory + harness state.',
    when: 'Após falha de dispatch, dispatcher timeout, sessão fechada no meio do EXECUTE.',
    notWhen: 'Spec terminou normalmente — use /complete em vez disso.',
    examples: ['/mustard:resume'],
    seeAlso: ['feature', 'bugfix', 'complete'],
  },
  {
    cmd: '/mustard:qa',
    syntax: '/mustard:qa',
    category: 'Pipeline',
    short: 'Executa a fase QA — roda todos os Acceptance Criteria.',
    simples: 'Roda os testes que você definiu na spec (Acceptance Criteria) e diz quais passaram e quais falharam. Esta etapa acontece automaticamente no pipeline, mas você pode rodar manualmente para validar antes do CLOSE.',
    tecnico: 'Lê seção "## Acceptance Criteria" da spec ativa, executa cada comando via execSync (Windows: cmd.exe), agrega resultados, escreve qa.result no harness log. close-gate bloqueia CLOSE se overall != pass (modo strict).',
    when: 'Antes de fechar a spec, ou ao iterar nos AC durante EXECUTE.',
    notWhen: 'Spec sem AC executável (defeito da spec, não do QA).',
    examples: ['/mustard:qa'],
    seeAlso: ['complete', 'feature', 'bugfix'],
  },

  // ── Tools (delegação) ────────────────────────────────────────────
  {
    cmd: '/mustard:task',
    syntax: '/mustard:task [prompt]',
    category: 'Tools',
    short: 'Despacha um agente para uma tarefa pontual sem spec.',
    simples: 'Quando você só quer "uma resposta" — analisar algo, refatorar um pedaço pequeno, explorar uma ideia — sem o peso completo do pipeline. Sem spec, sem aprovação, sem QA.',
    tecnico: 'Dispatch direto via Task tool com escopo escolhido (Explore, Plan, general-purpose). Não cria spec.md, não passa por gates de hygiene, não exige AC. Ideal para vibe/spike/prototype.',
    when: 'Análise rápida, prototipação, refactor pequeno isolado, exploração de código, code review pontual.',
    notWhen: 'Mudança que toca produção (schema, API, UI) — use feature em vez disso.',
    examples: ['/mustard:task analise toda a pasta src/auth e diga onde tem duplicação', '/mustard:task prototipe um botão glass com tailwind'],
    seeAlso: ['scan', 'review'],
  },
  {
    cmd: '/mustard:scan',
    syntax: '/mustard:scan [path]',
    category: 'Tools',
    short: 'Analisador agnóstico de código (estrutura, padrões, problemas).',
    simples: 'Roda uma varredura completa no código pra detectar padrões, convenções, possíveis bugs e oportunidades de melhoria. Não muda nada — só relata.',
    tecnico: 'Despacha agente Explore com refs progressive-disclosure. Detecta clusters (sufixos, decorators, function-prefix), mapeia entidades para entity-registry.json, valida convenções de naming (cluster-discovery.js). Output formatado segundo /mustard:scan-format.',
    when: 'Onboarding em projeto novo, audit periódico, antes de refactor grande.',
    notWhen: 'Já sabe o problema — vá direto para bugfix ou feature.',
    examples: ['/mustard:scan src/auth', '/mustard:scan'],
    seeAlso: ['scan-format', 'task'],
  },
  {
    cmd: '/mustard:scan-format',
    syntax: '/mustard:scan-format',
    category: 'Tools',
    short: 'Regras de formatação para output do /mustard:scan.',
    simples: 'Comando interno usado pelo /scan para formatar o relatório. Você raramente roda diretamente.',
    tecnico: 'Define o template de output do scan: agrupamento por arquivo, severidade, citação de linhas. Skill carregada pelo agente Explore quando dispatched via /mustard:scan.',
    when: 'Customizar formato de relatório.',
    notWhen: 'Uso normal — o /scan invoca automaticamente.',
    examples: ['/mustard:scan-format'],
    seeAlso: ['scan'],
  },
  {
    cmd: '/mustard:review',
    syntax: '/mustard:review [number|url]',
    category: 'Tools',
    short: 'Code review de um Pull Request.',
    simples: 'Aponte para um PR no GitHub e o Mustard faz uma revisão completa: lê os diffs, comenta problemas, sugere mudanças. Posta os comentários direto no PR.',
    tecnico: 'Invoca skill code-review com foco em correctness, security, performance e style. Lê PR via gh CLI ou URL. Gera findings priorizados (CRITICAL/WARNING/INFO) e posta como review comments. Emite eventos review.start e review.complete no harness para DORA metrics.',
    when: 'PR pronto para review, ou auto-review antes de pedir review humano.',
    notWhen: 'Código local não commitado — use /mustard:scan no diretório.',
    examples: ['/mustard:review 42', '/mustard:review https://github.com/org/repo/pull/42'],
    seeAlso: ['scan', 'metrics'],
  },
  {
    cmd: '/mustard:skill',
    syntax: '/mustard:skill [list|validate|new|edit] [name]',
    category: 'Tools',
    short: 'Gerencia skills do projeto (list, validate, criar nova).',
    simples: 'Skills são pacotinhos de instrução que o Claude carrega quando relevantes. Este comando lista as que você tem, valida se estão bem escritas, ou cria novas.',
    tecnico: 'Wrapper sobre scripts/skill-validate.js. Verifica YAML frontmatter (name único, description com gatilho), tamanho do body (≤200 ideal/≤500 max), header <!-- mustard:generated -->. Lista skills em templates/skills/ + .claude/skills/.',
    when: 'Criar skill nova, audit de skills existentes, validar antes de commit.',
    notWhen: 'Uso ad-hoc de skill — invoca direto via Skill(name).',
    examples: ['/mustard:skill list', '/mustard:skill validate karpathy-guidelines', '/mustard:skill new my-skill'],
    seeAlso: ['scan', 'maint'],
  },

  // ── Status & Metrics ─────────────────────────────────────────────
  {
    cmd: '/mustard:status',
    syntax: '/mustard:status',
    category: 'Status',
    short: 'Status consolidado do projeto.',
    simples: 'Mostra um resumo: branch atual, arquivos modificados, último commit, qual pipeline está ativo, último build, e quantas entidades estão registradas.',
    tecnico: 'Agrega: rtk git status + log, metrics-collect.js (active + orphaned pipelines), .claude/.last-build.json, entity-registry.json _meta. Não toca .pipeline-states/ direto — sempre via metrics-collect.js para consistência.',
    when: 'Início de sessão, antes de iniciar nova spec, antes de commit.',
    notWhen: 'Quer só métricas detalhadas — use /mustard:stats.',
    examples: ['/mustard:status'],
    seeAlso: ['stats', 'metrics'],
  },
  {
    cmd: '/mustard:stats',
    syntax: '/mustard:stats',
    category: 'Status',
    short: 'Estatísticas detalhadas: pipelines + hooks + RTK.',
    simples: 'Visão completa de tudo que aconteceu: quanto cada hook rodou, quanto o RTK economizou, performance dos pipelines, distribuição de tempo. Bom pra entender saúde do projeto.',
    tecnico: 'Superset de /mustard:metrics. Agrega .claude/.metrics/*.jsonl, .claude/.pipeline-states/*.metrics.json, rtk gain --format json. Cobre Pass@1, Last 7 Days, hook events, orphan detection. Renderiza markdown com tabelas e summaries.',
    when: 'Audit semanal/mensal, comparar antes/depois de mudanças, decidir onde economizar tokens.',
    notWhen: 'Só quer um glance rápido — use /mustard:status.',
    examples: ['/mustard:stats'],
    seeAlso: ['metrics', 'status'],
  },
  {
    cmd: '/mustard:metrics',
    syntax: '/mustard:metrics [--since DATE] [--event TYPE] [--compare A B] [--pr]',
    category: 'Status',
    short: 'Métricas focadas em hook events + comparação entre janelas.',
    simples: 'Visão menos detalhada que /stats, focada em eventos de hooks. Use --compare para ver delta entre duas datas/releases, ou --pr para métricas DORA.',
    tecnico: 'Wrapper sobre metrics-report.js. Suporta filtro --since e --event, comparação entre git tags (v1.2.3) ou ISO dates. --pr executa harness-views.js --view pr-metrics (lead time, review time, PR size, opened/merged per day, padrão DORA).',
    when: 'Avaliar impacto de uma mudança específica, gerar relatório DORA mensal.',
    notWhen: 'Quer ver tudo agregado — use /mustard:stats.',
    examples: ['/mustard:metrics', '/mustard:metrics --compare v3.1.21 v3.1.22', '/mustard:metrics --pr --days 30'],
    seeAlso: ['stats', 'status'],
  },
  {
    cmd: '/mustard:knowledge',
    syntax: '/mustard:knowledge [list|add|search|audit] [args]',
    category: 'Status',
    short: 'Gerencia a knowledge base do projeto.',
    simples: 'Knowledge é um arquivo onde o Mustard salva padrões e convenções aprendidas das suas specs. Use este comando para listar, buscar ou adicionar entradas.',
    tecnico: 'Manipula .claude/knowledge.json. Categorias: pattern, convention, entity, decision. Audit varre specs completed e propõe entries novas. Usado por session-knowledge hook para auto-extract.',
    when: 'Onboarding em projeto antigo, manter convenções alinhadas, gerar progress reports.',
    notWhen: 'Knowledge é gerenciado automaticamente pelos hooks na maioria dos casos.',
    examples: ['/mustard:knowledge list', '/mustard:knowledge search auth', '/mustard:knowledge audit'],
    seeAlso: ['scan', 'status'],
  },

  // ── Operations ───────────────────────────────────────────────────
  {
    cmd: '/mustard:git',
    syntax: '/mustard:git [sync|commit|push|merge]',
    category: 'Operations',
    short: 'Wrapper de git que segue o flow definido em mustard.json.',
    simples: 'Em vez de digitar comandos git complexos, este comando faz o passo certo automaticamente: sync puxa atualizações, commit cria um commit, push sobe para remoto, merge promove para a branch pai.',
    tecnico: 'Lê mustard.json para descobrir branch flow. sync = pull do parent. commit = simples. push = sync + commit + push. merge = fast-forward para parent (sem PR). merge main = promove dev → main explicitamente.',
    when: 'Operações git rotineiras seguindo flow do projeto.',
    notWhen: 'Operações destrutivas (rebase, force push, reset hard) — fazer manualmente para evitar acidentes.',
    examples: ['/mustard:git sync', '/mustard:git push', '/mustard:git merge main'],
    seeAlso: ['review', 'status'],
  },
  {
    cmd: '/mustard:dashboard',
    syntax: '/mustard:dashboard [start|stop|status]',
    category: 'Operations',
    short: 'Inicia/para o dashboard local (esta UI).',
    simples: 'Liga o servidor que serve esta página. Sem args, inicia se não estiver rodando. Sem subcomando = start.',
    tecnico: 'Spawn detached node .claude/scripts/dashboard.js (porta 7878). Grava PID em .claude/.dashboard.pid. Stop lê PID e mata processo. Probe HTTP HEAD para confirmar liveness.',
    when: 'Acompanhar specs em UI, criar PRDs visualmente, ajustar settings.',
    notWhen: 'CLI puro funciona melhor para scripting.',
    examples: ['/mustard:dashboard', '/mustard:dashboard stop', '/mustard:dashboard status'],
    seeAlso: ['stats', 'status'],
  },
  {
    cmd: '/mustard:maint',
    syntax: '/mustard:maint',
    category: 'Operations',
    short: 'Utilitários de manutenção (limpa órfãos, valida estado).',
    simples: 'Roda uma faxina: detecta specs perdidas, pipeline-states sem spec correspondente, cleanup de cache antigo. Não é destrutivo por padrão.',
    tecnico: 'Bulk reconcile de orphan pipelines (detecta .pipeline-states/<X> sem spec/active/<X>). Limpa .agent-memory/ velhas. Recompila entity-registry quando hash divergiu. Operações dry-run por padrão.',
    when: 'Periodicamente, ou quando /mustard:status reporta orphans.',
    notWhen: 'Pipelines em curso — espere terminar.',
    examples: ['/mustard:maint'],
    seeAlso: ['status', 'complete'],
  },
];

// Group by category for the UI
const CATEGORIES = ['Pipeline', 'Tools', 'Status', 'Operations'];

module.exports = { COMMANDS, CATEGORIES };
