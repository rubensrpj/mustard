---
name: mustard-feature
description: An internal flow — dispatched by the orchestrator router (CLAUDE.md § Intent Routing), not chosen directly by the user. Feature pipeline for a new entity or a change spanning ≥2 layers (ANALYZE → scope → inline EXECUTE for Light, or PLAN via the full-plan ref for Full). Weak fallback only: use when the router did not engage and the user asks to add, create, or implement a new feature.
source: manual
---
<!-- mustard:generated -->
# /feature - Feature Pipeline

**Iron law: NO code before the approved spec — the `scope_guard` gate will refuse it anyway.**

`/feature <request>` — understand the client, research the repo through the deterministic `scan` tool (never reading source by hand), then plan + implement.
Heavy lifting delegates to `mustard-rt`; the orchestrator routes phases + emits events. You (the AI) do the reasoning that grain cannot: elicitation, decomposition, lapidation.

## Rationalizations that don't fly

| Excuse | Answer |
|--------|--------|
| "it's just one line, no spec needed" | one line in two layers is a feature; the classifier decides, not the urge |
| "I already know the pattern — let me just write it" | knowing the pattern is PLAN input, not permission; `scope_guard` denies production edits before approval |
| "the digest missed, so this doesn't exist yet" | a `miss` is not "absent" — re-query with the repo's `candidates` vocabulary before concluding net-new |
| "reading the whole module is faster than the digest" | read ONLY the ~12 anchors; bulk-reading the repo is the cost this flow exists to avoid |
| "the user is in a hurry — skip the approval" | Full stops at PLAN; only `/spec` unlocks EXECUTE, and urgency does not change scope |

**Red flags** — catch yourself thinking any of these and return to the flow: *"I'll draft the spec after the code works."* · *"Scope came back full but it feels light — I'll execute inline anyway."* · *"No need to research; I remember where this lives."* · *"I'll hand-write `spec.md`; `spec-draft` is overhead."* · *"The gate blocked the edit — let me work around it."*

> **Progressive disclosure.** This file is the LIGHT path (most runs) + the shared ANALYZE. The Full-scope machinery — DECOMPOSE, the PLAN materialisation, the wave wiring and the Full-only rules — lives in **`../../../refs/feature/full-plan.md`**, read ONLY when scope detection returns `full`. Don't load it for a light task; that is the whole point of the split.

> **Artifact order.** The spec dir + `spec.md` + `meta.json` are MATERIALISED by `mustard-rt run spec-draft` (at scope detection below). They do **not** exist during research. The slug is born from `--intent` there. Never reference `.claude/spec/{slug}/spec.md` before it has been materialised.

## Action

### 1. Understand + RESEARCH (ANALYZE)

→ `../../../refs/feature/spec-hygiene.md`. (No stage emit here — there is no spec yet; `spec-draft` backfills the `ANALYZE` marker when the slug is born at scope detection.)

- **Note the client's intent** in your own words (what they actually want, plus every concrete critique from the conversation).
- **Ensure the model exists/is fresh**: `mustard-rt run scan` produces `.claude/grain.model.json` (the durable repo model). Run it if absent or the codebase changed materially.
- **Research via the scan digest, not the repo**: before the call, **lapidate the client's intent into code-shaped terms YOURSELF** (you hold the domain context — the project's `CONTEXT.md`, the subproject conventions, and you translate better than any inline model): strip the glue (prepositions/articles/connectors — keep only content words), translate each into the code's vocabulary, and shape them the way code NAMES things — verbs in the infinitive (`create`, `list`, `update`), collection nouns plural (`clients`, `contracts`, `receivables`, `installments`); **for a layer-bound task lead with the terms that name the WORK/layer (`datatable`, `column`, `cell`, `tooltip`, `badge`) over the shared domain nouns (`cpf`, `cnpj`, `validation`) that match every layer and let the wrong-layer plumbing out-score your target on frequency** (→ `../../../refs/locating-code.md`); when unsure of a form, include both (the digest dedups). Code-shaped terms land at the **EXACT** tier instead of leaning on `stem` — exactly where the anchor noise comes from, so this prunes the noise at the source. Then call `mustard-rt run feature --intent "<your lapidated code-shaped terms + the user's content words>"` — ONE call → the COMPACT *insumos* on stdout (`slices`, `contracts`, `hubs`, `anchors` + `anchorsDetail` — per-anchor provenance: the matched terms that declare each file (file→terms), so you pick what to read — `report.reason` (strong | weak | none | generated_only — the honest match strength) + `report.termCount` (the count; the full `report.terms` list is NOT in the compact stdout), `stacks` — detected stacks with confidence + the signals that grounded them, use to anchor framework-specific decisions — `miss`, `note`, and a `detail: ".claude/feature-digest.json"` pointer). **Read the compact stdout ONCE** — the binary ALSO writes the FULL digest (the complete `report.terms` and the `candidates` vocabulary menu) to `.claude/feature-digest.json` itself, so do NOT redirect/capture stdout to a file: when you need the long tail (full `report.terms`, the `candidates` menu, or any field dropped from the compact summary) read the already-written `.claude/feature-digest.json` (sliced — `offset`/`limit`), never re-run the command (each re-run re-computes the entire digest). The digest is **pure deterministic** (no model call, no nested `claude`): it tokenises the combined intent and matches the **distinct union** against the repo's term index. Passing BOTH is the point — the user's Portuguese hits the code's Portuguese, your translation hits the code's English, the union covers a mixed-language codebase, and a wrong guess simply matches nothing (the OR-query drops it). `strong` → use the anchors. `weak`/`none` → read the `candidates` term index from `.claude/feature-digest.json` (the repo's REAL vocabulary as `{term,samples,count}`); sharpen your translation against it and re-call. NEVER conclude "absent" from a `miss` alone — re-query with the code's words first. (Each query feeds `lexicon-suggest`, so a confirmed bridge becomes deterministic over time — fewer translations needed.)
- **Promote confirmed bridges**: after a successful re-query that followed a `weak`/`none` report, suggest running `mustard-rt run lexicon-suggest` — it correlates the recorded `feature.query` rounds across the whole workspace telemetry (so it also works from a later session) and lists the confirmed missed→bridged term pairs; nothing is written unless the user accepts one via `--accept <missed>=<bridged>` (project lexicon overlay only).
- **Prune, then read ONLY the surviving `anchors`** (~12 real files) — never the whole repo, never `grain.model.json` directly. First **prune by provenance**: each anchor in `anchorsDetail` carries the matched terms that surfaced it — drop the ones whose terms are tangential to the request (a seeder matched on `pagos`, a stats-DTO on `partner`), keep the central ones (route, form, datatable). Free judgment from data you already hold — no read, no model call. This is the low-consumption contract. **Prefer ONE consolidated `Task(Explore)` whenever the surviving anchors fit a single subagent** — a subagent saves the PARENT's context but still spends real tokens in the TOTAL bill, so one Explore reading all the anchors is cheaper than several. **Fan out to one `Task(Explore)` per subproject ONLY when the anchors genuinely span ≥2 subprojects with real volume in each** (not one stray anchor in a second subproject). The fan-out earns its extra subagents because every file you open in a subproject auto-injects that subproject's `CLAUDE.md` + skill catalog into your context — the most expensive, longest-lived one — so reading across N subprojects in the parent pays that fixed overhead N×, re-sent on every later turn; isolating each subproject in its own Explore keeps that out of the parent. Each Explore returns ≤40 lines (the pattern to mirror, files to touch, contract wiring). Reserve direct parent reads for a single-subproject feature whose few anchors don't justify even one subagent, and read only the slice you need (`offset`/`limit`), never whole files.
- **Lead with `slices` for a composition/enhancement.** When the task **reuses existing pieces** (the common enhancement — adding tabs, mirroring a panel), the `slices` are the PRIMARY signal: a slice names the recurring pattern (the route shape `[id]/new/PUT`, the hooks `useXByY`, the datatable) — *what to mirror* — which a flat anchor (a declaration site) cannot. The slice carries its **`exemplarFiles`** — the real files that exemplify the pattern — so go STRAIGHT to those (no Grep). Read the matching slice FIRST; treat the pruned `anchors` as the secondary, noisier signal. Only for a **net-new entity** do a sibling's anchors lead instead. (Field-confirmed across runs: on composition the slices repeatedly beat the flat anchors.)
- **Glossary grill (optional, non-blocking)** → `../../../refs/feature/glossary-grill.md`. After the digest, run `mustard-rt run glossary-coverage --intent "<request>" --context {root}/CONTEXT.md`; ONLY on `verdict ∈ {missing, weak}` run a LIGHT inline grill: for each term in `uncovered`, ask the user in chat for a one-line definition (one batched AskUserQuestion is fine), then persist each confirmed pair with `mustard-rt run grill-capture --term "<term>" --definition "<the user's words>" --context <contextFile from the coverage output>`. It is grounding the spec's language, not interrogating — keep it to ≤3 of the most central uncovered terms, and any term the user skips is dropped, never blocked on. `ok` / `na` / absent-tool → stay silent and continue. The sharpened `CONTEXT.md` flows downstream for free via the already-wired `context-slice`.
- **Specification check (selective grill) — an EARLY gate, before the heavy ceremony.** Judge whether the request is specified enough to PLAN without guessing. A request is *under-specified* when a concrete critique names an OUTCOME or SYMPTOM but not the MECHANISM or decision an implementer must make — e.g. "the quantity won't change / the values don't recalculate" leaves open: is the input disabled, read-only, or is its change handler broken? does the recompute belong client-side or in an endpoint? — or when the digest came back `weak`/`none` even after the repo-vocabulary re-query (the request does not yet name things the code knows). When EITHER holds (digest `weak`/`none`, OR the request is under-specified), **GRILL once NOW — before the scope/decompose ceremony of §2 and before any scope work**: ask the user 2-3 targeted questions (one batched AskUserQuestion) that resolve the open mechanism/decision, offering the concrete options you can already infer from the anchors so the user picks rather than writes — keep it didactic (plain words, no jargon). One batched question here resolves the open mechanism and PRUNES speculative research/validation you would otherwise pay for on a guess. Fold the answers into the noted intent (and downstream the spec's `context`/`acceptance-criteria`); a `weak`/`none` answer the grill sharpens may also warrant a re-call of the digest with the user's clarified words before §2. **This is SELECTIVE by design**: a concrete, well-covered request — a rename, a known slice to mirror, a critique that already names its target AND mechanism — SKIPS the grill entirely (no question, straight to §2/scope). The grill must pay for itself only on a vague request; it must never tax a concrete one (the routing economy). Distinct from the glossary grill above: that sharpens domain VOCABULARY; this resolves behavioral SPECIFICATION — the gap a digest hit alone cannot close (a request can match strong anchors and still be behaviorally ambiguous).

### 2. Route + scope (deterministic)

The digest located; route + scope are decided by the deterministic tools — never your eye alone. The flow goes straight from the pruned digest anchors into the steps below:

1. **Routing economy first.** If the pruned anchors show single-layer work with no new entity (most enhancements), the work does NOT need the pipeline → run it as **`/mustard:task`** (lean: no spec, no wave, no QA) on those anchors, then STOP.
2. Fill the kept census into the scaffold's `## Files`/`## Arquivos`, then `mustard-rt run spec-draft --intent "<request>" --scope <your light/full read> --lang <bcp47> [--query-terms "<repo-vocabulary terms when the raw words came back weak/none>"]` — the ONLY scaffold writer; its auto-downgrade gate is the deterministic backstop on the scope you passed.
3. `mustard-rt run plan-prepare --from-spec .claude/spec/{slug}/spec.md --slice-match-count <sliceMatchCount from the digest>` — the deterministic authority for `scope` (+ `decompose`/`waves`).
4. `mustard-rt run analyze-validation --spec .claude/spec/{slug}/spec.md`, append `issues[]` to `## Concerns` (WARN).
5. Emit `pipeline.scope` + `pipeline.stage: Plan`.
6. **`scope="light"`** → stay here and EXECUTE (§3). **`scope="full"`** → open **`../../../refs/feature/full-plan.md`** and follow PLAN. Stop reading this file.
7. **Multiple units** — when the digest's `concerns` show ≥2, each is its own unit, scoped to its anchors (Full: a wave; light/task: its own dispatch).

(Scope labels for orientation — the DECISION is `plan-prepare`'s `scope` (with `spec-draft`'s auto-downgrade gate as the deterministic backstop), never your eye: light = 1-2 layers, ≤5 files, mirrors a slice | extended-light = matched slice + modifies existing, 6-8 files | full = 3+ layers, net-new, spans ≥2 slices *with* ≥2 layers, or >8 files.)

### 3. Light/Extended-Light EXECUTE (inline)

**Light / Extended-Light only.** Full scope never reaches this step — it routed to `../../../refs/feature/full-plan.md` and resumes through `/spec`.

User chooses "Approve and implement now": emit `pipeline.stage: Execute` → `exec-rewave-check` (decomposed → use wave-1 spec) → `dependency-precheck` (block on missing externals) → dispatch agents via `agent-prompt-render --emit ref` (NEVER hand-craft; the 2-line stub stdout IS the Task prompt — pass it verbatim, the PreToolUse hook expands it so the full prompt never transits your context; all agents of a wave → one message; the subagent's context is the spec's project section + its anchors; dispatch each with its role's `subagent_type` — `review`→`mustard-review` (read-only), `impl`→`general-purpose`) → per-wave validate + `memory agent` → REVIEW per subproject (`review-result` emit, max 2 fix loops) → QA (`qa-run`; pass → CLOSE; fail → return failing AC; skip → warn + allow CLOSE; max 3 QA iterations).

Before EXECUTE, present the plan WITH the approval question — **NEVER ask about a plan the user has not seen.** Text emitted before a tool call may not render (background sessions), so print the spec in the final message AND attach it as the `preview` of the approval option in AskUserQuestion: **"Approve and implement?"** / **"Adjust (give feedback)"** / **"Save for later (stop)"**.

Escalations: `Internal error` (transient dispatch failure, e.g. "Tool result missing due to internal error") → re-dispatch once; still failing → STOP (the Light spec is tracked — resume via `/spec`, which retries per `../../../refs/spec/resume-loop.md`). `CONCERN`/`BLOCKED`/`PARTIAL`/`DEFERRED` → `../../../refs/spec/resume-loop.md § Escalation`. AC cross-shell quirks → `../../../refs/feature/ac-cross-shell.md`.

## INVIOLABLE RULES (all scopes)

- ALWAYS research via `mustard-rt run feature` (the scan digest) — never read the repo or `grain.model.json` WHOLE to understand it. The digest finds where to look; it does not replace reading the files it points to.
- The digest is **pure deterministic** — it NEVER calls a model. Pass `--intent` with **the user's own words PLUS your code-vocabulary translation** (you do the translation at the orchestration layer; the digest matches the distinct union). On `weak`/`none` the digest returns the `candidates` term index — sharpen your translation against it and re-call. A `miss` is NOT "absent": re-query with the code's vocabulary before concluding net-new; treat true net-new as DESIGN, not recomposition.
- READ ONLY the `anchors` the scan tools point to (~12 files). NEVER bulk-read source — but follow an anchor's references when the question is about composition. Settle existence/duplication questions by Grep enumeration BEFORE any subagent — sampled reading never proves absence; the Full-scope pre-EXECUTE existence gate is in `../../../refs/feature/existence-gate.md`. When anchors cross ≥2 subprojects, delegate the READING to one `Task(Explore)` per subproject (keeps each subproject's `CLAUDE.md`+skill catalog out of the parent context), rendering each prompt via `agent-prompt-render --role explore --task-text ... --emit ref` (pass the 2-line stub stdout verbatim). **TRUST each subagent's briefing — it returns a condensed summary you ACT on, not a hint to re-verify (Anthropic's sub-agent contract).** Re-read directly ONLY to settle a conclusion that CONTRADICTS the user or claims something is ABSENT (the Verdict rule); NEVER re-ground a finding it already delivered with `file:line`.
- The spec scaffold (`spec.md` + `meta.json`) is materialised ONLY by `mustard-rt run spec-draft`. NEVER hand-write `spec.md` with the Write tool, and NEVER Read back a spec/scaffold/`meta.json` you just wrote — Write/Edit confirmed it; re-reading is pure token round-trip.
- NEVER hand-craft agent prompts — always `agent-prompt-render`; the subagent's context is the spec section + anchors. Dispatch each agent with the `subagent_type` the tool recommends per role (`explore`→`Explore`, `review`/`qa`→`mustard-review`, `guards`→`mustard-guards`; writing roles → `general-purpose`).
- NEVER skip `analyze-validation` or `dependency-precheck`.
- ALWAYS emit `pipeline.scope` + `pipeline.stage` at each transition. **The exact commands — there is NO `run emit`, never guess it:** scope → `mustard-rt run emit-pipeline --kind pipeline.scope --spec {slug} --payload <json>`; stage/phase → `mustard-rt run emit-phase --spec {slug} --to {Phase}` (e.g. `--to Plan` / `--to Execute`).
- **Full scope rules live in `../../../refs/feature/full-plan.md`** — Full-stops-at-PLAN (no inline EXECUTE; `/spec` approves), the `scope_guard` hard-gate, wave-body authoring, and `scan spec` for net-new units. Read them there when scope = full.
