# Memory — Deep refactor followups — FTS5 hyphen UX, flaky integration tests, dashboard i18n hardcode, economy baselines instrumentation, init banner adapt-cursor mention

Princípios técnicos formalizados durante esta spec.

## Princípios (0)

Nenhum princípio registrado ainda. Adicione via:

```
mustard-rt run spec-memory create --spec deep-refactor-followups-fts5-hyphen-ux-flaky-integration-tes --name <kebab> --kind principle --origin-wave wave-N-<role>
```
