## Verdict — Wave 1 of `close-eleven-harness-defects-found`: APPROVED

**AC-1 — PASS.** `rtk cargo test -p mustard-rt close_reports_a_still_red_criterion_without_withholding` → `2 passed` (nonzero). Close reports the still-red criterion by id with its column's wording and still completes. The advisory direction matches the AMENDED AC-1: I independently confirmed the amendment provenance in `.claude/spec/close-eleven-harness-defects-found/ac-proof.json` (`amendments[0]` names the superseded refusal test and the 2026-07-29 user decision), and it matches the DECISIONS constraint that the confirmation gate must not block.

**AC-2 — PASS.** `close_takes_the_removal_pass` → `2 passed`. `close()` in `apps/rt/src/commands/pipeline/close_pipeline.rs` now calls `ac_negative_check::take_removal` (made `pub(crate)` for exactly this caller) and refuses only on `Removal::Survived`; engine errors report `taken:false, ok:null` and never withhold — two-sided in the test (real git repo, real scratch tree).

**AC-3 — PASS.** `control_command_must_be_green_today` → `2 passed`. Three-sided at `apps/rt/src/commands/review/ac_negative_check.rs:1797`: red control refuses before the red proof is taken; green control lets the same red command prove; absent control lands in `control_missing` naming the id, trailing exempt criterion excluded. Ledger read-only at approval (`approve_spec.rs` consults `p.control` off the record, re-runs nothing).

**AC-4 — PASS.** `wave_claiming_a_criterion_must_contain_its_paths` → `2 passed`. The sufficiency list now travels in `ScaffoldOutcome::Created` and `plan-materialize` maps `ERR_CRITERIA_OUTSIDE_CLAIMANTS` to exit 2; declared globs are matched through the one existing matcher (`boundary_gate::glob_match`, visibility-only change), two-sided (covering glob clears, foreign glob still flags). Note: the AC says "expanded against the tree"; the implementation pattern-matches instead of walking the tree — documented in-code as the byte-stability choice, satisfies the intent (never compared literally). Minor, not blocking.

**AC-5 — PASS.** `placeholder_matches_the_skeleton_token_not_any_angle_bracket` → `2 passed`. Single predicate `qa_run::is_skeleton` (`apps/rt/src/commands/review/qa_run/mod.rs:467`); I verified zero remaining `contains('<')` copies in the four consumer sites — `ac_negative_check` (x2), `analyze_validation` (x2), `complete_spec` all route through it. Generics, quoted tags, redirections and heredocs now run; both localized skeleton literals still refuse.

**AC-6 — PASS.** `weak_ac_defers_to_the_recorded_proof` → `2 passed`. The linter reads the ledger through the producer's own doors (`load_ledger`/`recorded_proof`/`evidenced`) — the judgement lives in the consumer, no shared-status change, consistent with the shared-status-coupling law. Two-sided (unmeasured search still flags).

**Regression net:** `rtk cargo test --workspace` → **4468 passed, 0 failed, 6 ignored (63 suites)** — includes the insta byte-stability snapshots, `run_command_surface` lock and `template_parity` reverse ratchet (no new subcommand was added, so the four-registration guard is not triggered). `cargo clippy -p mustard-rt --all-targets` exits 0 with zero error-level diagnostics — the `unwrap_used`/`expect_used` deny holds; the only warning in a touched file (`approve_spec.rs:1237`) pre-exists on main and sits in `#[cfg(test)]`.

**Guards/molds:** no hook decision path was altered (`boundary_gate.rs` change is `pub(crate)` visibility + doc only); no gate/check/inject/observer/redirect module was created or refactored, so no mold applies. New close-report fields are ordered JSON derived from sorted/plan-ordered inputs. The wave file-list named `spec_draft.rs` but the edit landed in `spec_scaffold.rs` — justified: `spec_draft.rs:484` delegates to `spec_scaffold::write_spec_md`, and the seeding test proves round-trip through the shared parser.

**Change requests:** all three are conversational dispatch messages ("o que você me sugere?", "sim, executa", "segue com o review/qa da onda 1") — none carries a code-level request; nothing dropped.

Minor (non-blocking) observations:
- `apps/rt/src/commands/wave/wave_scaffold.rs:598` — sufficiency matches globs against the criterion's named path instead of expanding against the tree as AC-4's prose says; deliberate, documented, test-covered.
- `apps/rt/src/commands/review/qa_run/mod.rs:467` — `is_skeleton` is a boundary-anchored `<…>` shape test, broader than the exact i18n literal the evidence pointed at; the breadth is needed for hand-written placeholders (`<test-name>`) and the two-sided test pins both directions.

<VERDICT>{"verdict":"approved","critical":0,"findings":[{"severity":"minor","location":"apps/rt/src/commands/wave/wave_scaffold.rs:598","summary":"AC-4 wording says wildcards expanded against the tree; implementation pattern-matches (documented byte-stability choice, test-covered)"},{"severity":"minor","location":"apps/rt/src/commands/review/qa_run/mod.rs:467","summary":"is_skeleton is a shape heuristic broader than the exact drafter literal; justified for hand-written placeholders, two-sided test pins it"}]}</VERDICT>
