## Verdict — approved (critical: 0)

T1-T3 all PASS: one URL spelling for view+evidence (refactored to share); honest reduction (empty = measured Absent, failures = Unknown, never fabricated); azure routed before the github guard, others still unsupported; compose_azure pure with key-for-key shape parity against the github document; diff-unreadable refusal honored. No unwrap/expect outside tests. pr_azure 9, branch_state 11, review_prefetch 13; 36 integration suites ok; lib 2020/2021 (the 1 is the pre-existing writer_ndjson latency smoke, untouched).

Minor (non-blocking): the diff-unreadable refusal message has no direct unit test; the writer_ndjson latency smoke fails on WSL2 under load (pre-existing).
