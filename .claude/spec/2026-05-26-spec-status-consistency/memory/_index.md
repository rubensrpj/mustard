# Memory — spec-status-consistency — padronizar geração e atualização de status das specs

Princípios técnicos formalizados durante esta spec.

## Princípios (0)

Nenhum princípio registrado ainda. Adicione via:

```
mustard-rt run spec-memory create --spec spec-status-consistency-padronizar-gera-o-e-atualiza-o-de-st --name <kebab> --kind principle --origin-wave wave-N-<role>
```
