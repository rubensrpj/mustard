# Change Log — validation-that-does-not-lie

_Solicitações registradas automaticamente durante o pipeline (mid-spec). O `spec.md` (narrativa congelada) NÃO é alterado; dobre o que muda comportamento em `## Acceptance Criteria` e rode o QA de novo._

- **2026-07-24T16:56:10.195Z** _(Plan)_ — <task-notification> <task-id>a1dcefa3cfffbd510</task-id> <tool-use-id>toolu_01SitzNUot44a3azjxPvo1TL</tool-use-id> <output-file>C:\Users\ruben\AppData\Local\Temp\claude\C--Atiz-mustard\c8463934-5406-48ec-a215-20761204a738\tasks\a1dcefa3cfffbd510.output</output-file> <status>completed</status> <summary>Agent "Onda 2 wave 1 — file validation" finished</summary> <note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note> <result>Verification complete. The result is unambiguous: **there is no implementation to review.** ## Verdict — wave-1-review, spec `validation-that-does-not-lie` **Timing note first (important for the caller):** the wave-1 implementer was launched **asynchronously** at `2026-07-24T16:53:04Z` (`agent.stop` payload: `{"isAsync":true,"status":"async_launched","agentId":"a1dcefa3cfffbd510","description":"Onda 2 wave 1 — file validation"}`). My review was dispatched ~in parallel. At `2026-07-24T16:55:42Z` the working tree still carried **zero** changes under `apps/rt`. Either the implementer is still running or it produced nothing — either way the review was dispatched before there was anything to verify. ### Evidence `git status --short` → only `?? .claude/spec/validation-that-does-not-lie/`. `git diff --stat -- apps/rt` → empty. `analyze_validation.rs` mtime `24/07/2026 12:52:25` = checkout time; last commit touching it is `d1c15feb`, an unrelated harness fix. **Task 1 (root as parameter) — NOT DONE** - `apps/rt/src/commands/review/analyze_validation.rs:394` — `pub fn validate(abs_path: &amp;Path, content: &amp;str) -&gt; Vec&lt;Value&gt;` (no `root` param) - `:425` and `:472` — both `std::env::current_dir().unwrap_or_else(|_| PathBuf::from("."))` sites intact - `:193` — `|| fs::exists(Path::new(r))` unchanged inside `ref_resolves` - Callers unchanged: `analyze_validation.rs:642`, `apps/rt/src/commands/pipeline/plan_materialize.rs:226`, `apps/rt/src/commands/spec/spec_draft.rs:1305` — all still `validate(&amp;x, &amp;y)` with two args **Task 2 (widen token charset) — NOT DONE** - `:172` — `.all(|c| c.is_ascii_alphanumeric() || matches!(c, '.' | '/' | '-' | '_'))`. `(`, `)`, `[`, `]`, `{`, `}`, `*` still disqualify the whole token. **Task 3 (tighten what counts as a reference + state the rule in module docs) — NOT DONE** - Module docs (`:1`–`:6`) unchanged; the `KNOWN_FILE_EXTS` gate comment at `:137`–`:141` is the pre-existing text. No new rule stated. **Task 4 (three top-level integration tests) — NOT DONE** - `grep` for the three names hits only the two spec markdown files, no Rust source. - All three AC commands were run. Each returns `running 0 tests` / `0 passed` across every binary; filtering the output for the declared `Expect:` regex `1 passed` returns **nothing** for AC-1, AC-2 and AC-3. Note this is exactly the failure mode AC-1..AC-3 were designed to catch — and worth flagging: `cargo test … -- --exact` exits **0** with `0 passed`, so a naive gate reading only the exit code would have passed this wave. The `Expect: 1 passed` regex on each AC is what makes the emptiness visible; keep it. MOLD CONTRACT: not applicable — no file was created or refactored, so no `{role}-pattern` skill has a subject. Recommendation: re-dispatch the wave-1 implementer, wait for its completion, then re-run this review. &lt;VERDICT&gt;{"verdict":"rejected","critical":4,"findings":[{"severity":"critical","location":"apps/rt/src/commands/review/analyze_validation.rs:394","summary":"Task 1 not implemented: validate() still takes no root param; current_dir() sites at :425 and :472 intact; ref_resolves :193 still uses Path::new(r); callers at :642, plan_materialize.rs:226 and spec_draft.rs:1305 unchanged"},{"severity":"critical","location":"apps/rt/src/commands/review/analyze_validation.rs:172","summary":"Task 2 not implemented: backtick_file_refs char class still rejects ( ) [ ] { } and *, so route-group and dynamic-segment paths are silently dropped"},{"severity":"critical","location":"apps/rt/src/commands/review/analyze_validation.rs:1","summary":"Task 3 not implemented: no tightening of what counts as a reference and no rule stated in the module docs"},{"severity":"critical","location":"apps/rt/tests:0","summary":"Task 4 not implemented: validation_resolves_from_any_working_directory, validation_sees_paths_with_punctuated_segments and validation_does_not_flag_prose_as_a_file do not exist; all three AC commands report 0 passed and never emit the declared Expect regex '1 passed'"}]}&lt;/VERDICT&gt;</result> <usage><subagent_tokens>88059</subagent_tokens><tool_uses>19</tool_uses><duration_ms>182755</duration_ms></usage> </task-notification>
- **2026-07-24T17:12:08.224Z** _(Plan)_ — <task-notification> <task-id>a9e7a9648301ca1d1</task-id> <tool-use-id>toolu_01XmgSgfq9d5WvrLt5uKa4mc</tool-use-id> <output-file>C:\Users\ruben\AppData\Local\Temp\claude\C--Atiz-mustard\c8463934-5406-48ec-a215-20761204a738\tasks\a9e7a9648301ca1d1.output</output-file> <status>completed</status> <summary>Agent "Onda 2 wave 2 — scaffold census" finished</summary> <note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note> <result>All four tasks are complete and verified. ## Report **Files changed (all under `apps/rt/`)** - `src/commands/scan_guards/list.rs` — lifted the walk into `collect_pending(root) -&gt; PendingScaffolds { entries, errors }`, now the single owner of traversal + sorting. `run()` is reduced to the JSON-worklist projection; `Pending` and its fields became `pub(crate)`. The dead `#[cfg(test)] run_collect` shim is gone (tests call the real collector). No second walk, no second ignore list. - `src/commands/doctor/guards_scaffold_check.rs` (new) — `doctor --check guards-scaffold`. Follows rt-check-pattern: module doc naming the invocation + fail-open story, `pub fn run(root) -&gt; Option&lt;GuardsScaffoldReport&gt;`, pure `project_uncurated()` split from `build_report()`, sorted vectors, repo-relative paths, no floats/timestamps, in-file tests. - `src/commands/doctor/mod.rs` — module registration. - `src/commands/doctor/doctor.rs` — added `guards-scaffold` to the typed-check selector, `run_typed_check`, the unknown-check hint, the full run (advisory), `render_combined_json` (`guards_scaffold` key, omitted when there is no census), and `guards_scaffold_to_check_result` (WARN only, never FAIL). **No new `run` subcommand** — `tests/run_command_surface.rs` passes untouched. - `src/commands/agent/render/sections.rs` — `read_guards_block` prefixes `&gt; NOTE: …the uncurated /scan scaffold…There are NO project rules below…` when the body still carries `GUARDS_PENDING_OPEN` (single-sourced from `scan_claude`). Prefix, not swap: the raw block survives. Fail-open intact — absent file/section still yields `""` (no injection, no bare notice). - `src/commands/agent/render/mod.rs` — `sections` is now `pub mod` (commented as test-face only); only `read_guards_block` was widened to `pub`. - `tests/doctor_reports_uncurated_rule_scaffolds.rs`, `tests/dispatch_warns_on_uncurated_rules.rs` (new) — top-level `#[test]` fns at binary root, both two-sided. **Non-obvious decisions** 1. The collector also returns an `errors` vector. Without it the check's `scanned_errors` would be structurally always-empty (decorative), and an unreadable `CLAUDE.md` would read as "enriched". The worklist projection ignores it, so `scan-guards-list` output is byte-identical (verified: still `[]` on this repo). 2. "No census" = `.claude/grain.model.json` absent → `run` returns `None`, mirroring `capability_drift_check`. Without Wave 1 nothing can carry the sentinel, so "0 uncurated" would be a vacuous green. 3. No `HarnessEvent` is emitted — `mustard-core` publishes no event kind for this finding, and adding one is outside apps/rt. 4. Fixturing an unreadable `CLAUDE.md` uses invalid UTF-8, not a directory: `walk()` recurses into a dir named `CLAUDE.md` rather than classifying it. **Verification**: `cargo clippy --all-targets` clean on all touched files; `cargo test --lib` 1612 passed; both new integration tests pass under `-- --exact`; `run_command_surface` 7/7; live `doctor --check guards-scaffold --format json` → `{"ok":true,…}`, exit 0. **Blocker encountered (resolved, not mine)**: mid-run the lib stopped compiling with 3 `E0061`s in `src/commands/review/analyze_validation.rs` (plus a call site in `pipeline/plan_materialize.rs`) — a concurrent edit to a file I do not own. It cleared on retry; no action taken on those files. No doc surface outside `apps/rt` lists doctor check names (only the shipped `plugin/bin/*.exe` artifacts), so nothing outside my scope needs updating.</result> <usage><subagent_tokens>193789</subagent_tokens><tool_uses>63</tool_uses><duration_ms>1136249</duration_ms></usage> </task-notification>
- **2026-07-24T17:16:07.223Z** _(Plan)_ — <task-notification> <task-id>af2fdc5d9c0cca278</task-id> <tool-use-id>toolu_01LM58ff3mL4TttpgLPNoAbS</tool-use-id> <output-file>C:\Users\ruben\AppData\Local\Temp\claude\C--Atiz-mustard\c8463934-5406-48ec-a215-20761204a738\tasks\af2fdc5d9c0cca278.output</output-file> <status>completed</status> <summary>Agent "Onda 2 wave 1 — implement validation" finished</summary> <note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note> <result>All four tasks are implemented, the full `mustard-rt` suite is green (3291 passed), clippy is clean on the touched files, and the workspace builds. ## Files changed - `C:\Atiz\mustard\apps\rt\src\commands\review\analyze_validation.rs` - `C:\Atiz\mustard\apps\rt\src\commands\pipeline\plan_materialize.rs` - `C:\Atiz\mustard\apps\rt\src\commands\spec\spec_draft.rs` - `C:\Atiz\mustard\apps\rt\tests\spec_draft_context_prose.rs` (call-site update) - `C:\Atiz\mustard\apps\rt\tests\validation_file_refs.rs` (new — the three AC tests) ## What was done 1. **Root as a parameter.** `validate(root: &amp;Path, abs_path: &amp;Path, content: &amp;str)` — unconditional, no `Option`, no internal fallback. Both `current_dir()` sites are gone; the second one (V4, extended-light) now reuses the *same* `model` path V2 computes, so there is one project root and one model per run. `ref_resolves` takes `root` and uses `root.join(r)` instead of `Path::new(r)`. Callers pass `crate::shared::context::project_dir()`; `plan_materialize` threads down the `project` root `materialize` already holds (`validate_root_spec(project, spec_dir)`). 2+3. **Charset widened and reference test tightened together**, split into two named functions so the rule is legible: `is_path_token_char` (adds `( ) [ ] { } *`) and `is_file_reference`. The rule, stated in full in the module docs: a token is a reference to one concrete file when it is all path chars, carries no pattern metacharacter (`*`, `{`, `}`), no `//` and no `...` elision segment, and its last segment splits into a non-empty stem plus a clean extension that is either known or accompanied by a `/`. Brackets are deliberately *not* pattern chars — `app/[slug]` is a literal directory. I checked the rejected shapes against real specs in this repo rather than inventing them: `.../spec.md` (`btw-plan-rework-fixes/wave-1-rt/spec.md:22`) is flagged *today* from the correct root, and `wave-N-{role}/spec.md` (same file, line 17) plus `plugin/**/*.md` (`wave-2-docs/spec.md:20`) would have become false warnings the moment the charset widened. Bare `.tsx`-style extension mentions were the fourth class. 4. **Three top-level `#[test]` fns** in one new integration binary, each two-sided. The cwd test is the one worth flagging: besides "existing file resolves / absent file warns", it also asserts that `Cargo.toml` — which exists in the *process* working directory but not under the passed root — is still reported missing. That is a direct proof `current_dir()` is no longer consulted, and it needs no `set_current_dir` (which would race with parallel tests in the same binary). ## Verification Each AC command run verbatim; all three emit the declared `Expect: 1 passed`: `test result: ok. 1 passed; 0 failed; ...; 2 filtered out` for all of `validation_resolves_from_any_working_directory`, `validation_sees_paths_with_punctuated_segments`, `validation_does_not_flag_prose_as_a_file`. ## Notes - **MOLD CONTRACT:** no `{role}-pattern` skill applies. `analyze_validation.rs` is a `commands/review/` run subcommand, not a `doctor/{name}_check.rs`; `rt-check-pattern` is the nearest mold and its `pub fn run(root: &amp;Path)` convention is exactly the direction task 1 moves in. The new file is an integration test, which no mold covers. - No blockers. The doctor check and dispatch warning (parent spec root causes 4) belong to wave 2 and were left untouched. &lt;MEMORY&gt;To prove a function stopped reading a process global like current_dir(), assert that an input resolvable ONLY via that global still fails — e.g. reference `Cargo.toml` (present in the cargo test cwd, absent under the passed root) and require the warning. Avoids `set_current_dir`, which races with parallel tests in the same binary.&lt;/MEMORY&gt;</result> <usage><subagent_tokens>150345</subagent_tokens><tool_uses>43</tool_uses><duration_ms>1133192</duration_ms></usage> </task-notification>
- **2026-07-24T17:19:24.538Z** _(Plan)_ — <task-notification> <task-id>bqg01s8lh</task-id> <tool-use-id>toolu_01AXg65EkR8qPfmoNVecwrqw</tool-use-id> <output-file>C:\Users\ruben\AppData\Local\Temp\claude\C--Atiz-mustard\c8463934-5406-48ec-a215-20761204a738\tasks\bqg01s8lh.output</output-file> <status>completed</status> <summary>Background command "Run the QA gate for Onda 2" completed (exit code 0)</summary> </task-notification>
