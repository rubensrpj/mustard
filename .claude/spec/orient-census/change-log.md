# Change Log — orient-census

_Solicitações registradas automaticamente durante o pipeline (mid-spec). O `spec.md` (narrativa congelada) NÃO é alterado; dobre o que muda comportamento em `## Acceptance Criteria` e rode o QA de novo._

- **2026-07-03T21:57:55.067Z** _(Analyze)_ — <task-notification> <task-id>b1v6s7d9s</task-id> <tool-use-id>toolu_01GF3kAK7CSHYjuxcKLDtoF3</tool-use-id> <output-file>C:\Users\ruben\AppData\Local\Temp\claude\C--Atiz-mustard\4aca25b4-2e93-4f0d-9caf-f216dd9e6b4e\tasks\b1v6s7d9s.output</output-file> <status>completed</status> <summary>Background command "Procurar comandos de census/map no rt" completed (exit code 0)</summary> </task-notification>
- **2026-07-03T21:58:10.997Z** _(Analyze)_ — <task-notification> <task-id>bpwe2utdw</task-id> <tool-use-id>toolu_01GcpNfWKa8XoyxFgY1TQ3VX</tool-use-id> <output-file>C:\Users\ruben\AppData\Local\Temp\claude\C--Atiz-mustard\4aca25b4-2e93-4f0d-9caf-f216dd9e6b4e\tasks\bpwe2utdw.output</output-file> <status>failed</status> <summary>Background command "Ler git.flow do mustard.json" failed with exit code 1</summary> </task-notification>
- **2026-07-03T21:58:12.911Z** _(Analyze)_ — <task-notification> <task-id>af8787e1ff28e064e</task-id> <tool-use-id>toolu_01SAQ7fFHZUfQWkeWV7C7fa6</tool-use-id> <output-file>C:\Users\ruben\AppData\Local\Temp\claude\C--Atiz-mustard\4aca25b4-2e93-4f0d-9caf-f216dd9e6b4e\tasks\af8787e1ff28e064e.output</output-file> <status>completed</status> <summary>Agent "Implementar census de orientação" finished</summary> <note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note> <result>All acceptance criteria pass. Here is the summary. ## Done — census de orientação implemented, all ACs green **Files touched** - `apps/rt/src/commands/orient.rs` (NEW) — `compute_orientation(root, intent)` core + `render_terrain`/`render_entrypoints` + `run()` command + minimal serde read-view of the grain model + tests (2 insta inline snapshots, `orient_fail_open`, cap test, no-intent test). - `apps/rt/src/commands/mod.rs` — `pub mod orient;`, `RunCmd::Orient { intent, root }` variant, and the `dispatch()` arm (both registrations, per the rt Guard). - `apps/rt/src/hooks/session/session_start_inject.rs` — Level 1 (Terrain) injected, composed with the existing memory payload (either/both/neither injects). - `apps/rt/src/hooks/session/prompt_submit_inject.rs` — Level 2 (Entrypoints) injected in the complementary branch: not `/mustard:*` AND no active spec (banner branch kept for active-spec). - `apps/cli/templates/CLAUDE.md` + `apps/cli/templates/refs/locating-code.md` — prose swapped to "terrain is already in your window (the census); digest only for concept beyond it". - `apps/rt/src/commands/scan_claude.rs` — the generated subproject `## Guards` footer line ("Pesquise via…") swapped to the census framing. - `apps/rt/Cargo.toml` — `insta` dev-dependency (needed for the required snapshot test). **Build/test** - `cargo build`: 0 errors (AC-5). `cargo test -p mustard-rt`: **3180 passed** (AC-4), incl. 2 insta snapshots + `orient_fail_open` (AC-3). `clippy --all-targets`: 0 errors, no `orient` warnings, no `unwrap`/`expect` outside `#[cfg(test)]`. - AC-1 (real repo): 7 byte-stable terrain lines (`dashboard·npm·195 — L0` … `(root)·npm·12 — L2`). AC-2 (real repo): only `apps/rt`, 4 `path:line — role` lines, no snippet. AC-3: `run orient --root &lt;empty&gt;` → empty output, exit 0. **Design decisions** - **Core fn location**: in `commands/orient.rs` as `pub fn`; both injects call `crate::commands::orient::…` directly (no facade — matches existing hooks→commands imports). - **Grain source**: parse `.claude/grain.model.json` directly with a minimal read-view (reusing `mustard_core::domain::scan::Project` for `projects[]`; local `Convention`/`Example`/`SkeletonEntry`). Chosen over `scan facts` because facts doesn't expose `conventions`/`examples`/`skeleton`, and direct parse is deterministic with no PATH/subprocess dependency. The `Example` view **omits `snippet`** entirely, so a code body can never leak. - **Type+count**: reused `Project.kind` + `Project.code_files` — the exact fields the `scan_claude` footer renders. - **"papel" (Level 1 role)**: grain's `skeleton[]` layer (L0/L1/L2), joined by dir (root `""` ↔ `"(root)"`). This join also filters Level 1 to real architectural units (test fixtures + nested inner crates drop), keeping terrain ~7 lines; fallback: empty skeleton → keep all projects, blank role. - **Intent match**: lexicon = subproject **name + dir-leaf tokens only**. I first included `conventions[].entities` but grain's slices span multiple subprojects (entity `rt` lives in a slice whose examples sit under `apps/dashboard/src-tauri`), which leaked `rt` into dashboard and matched 2 subprojects for the AC-2 intent — violating "só do subprojeto casado". Name/dir is the specific non-leaky anchor; requests that don't name a subproject fall through to Level 1 + digest (the intended split). Conventions are still used to *collect* the path-scoped exemplars. Caps: ≤2 subprojects (score→size→name), ≤4 roles (BTreeMap by role, smallest path wins). **Note**: two edits are cascades outside the spec's `## Files` and tripped the boundary gate (WARN, non-blocking): `apps/rt/Cargo.toml` (insta) and `apps/rt/src/commands/scan_claude.rs` (deliverable #5's footer). No commit made (left for the orchestrator).</result> <usage><subagent_tokens>200589</subagent_tokens><tool_uses>60</tool_uses><duration_ms>1862126</duration_ms></usage> </task-notification>
