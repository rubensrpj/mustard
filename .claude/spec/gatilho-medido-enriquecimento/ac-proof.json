{
  "spec": "gatilho-medido-enriquecimento",
  "criteria": [
    {
      "id": "AC-1",
      "command": "cargo test -p mustard-rt --lib commands::event::enrichment_gap::tests::counts_molds_with_no_author -- --exact 2>&1 | grep -q \"test result: ok. 1 passed\"",
      "expect": null,
      "control_command": "cargo test -p mustard-rt --lib base_gate 2>&1 | grep -q \"test result: ok\"",
      "verdict": "proven",
      "proof": "red",
      "control": "green",
      "control_exit": 0,
      "confirmation": "green",
      "exit": 1,
      "confirmation_exit": 0,
      "removal": "evidence-removed",
      "removal_exit": null,
      "reason": "the REMOVAL was NOT TAKEN: this criterion's own evidence (its command or its `Expect:` regex) names `enrichment_gap`, which the strip itself deleted from the tree, so the command was going to come back red whatever the behaviour does — that red would be a fact about the strip, not about the criterion. This transition has nothing to say here; it speaks only about a criterion whose evidence outlives the work it checks",
      "stderr_excerpt": ""
    },
    {
      "id": "AC-2",
      "command": "cargo test -p mustard-rt --lib commands::event::enrichment_gap::tests::names_a_subproject_whose_guards_are_still_a_scaffold -- --exact 2>&1 | grep -q \"test result: ok. 1 passed\"",
      "expect": null,
      "control_command": "cargo test -p mustard-rt --lib scan_guards 2>&1 | grep -q \"test result: ok\"",
      "verdict": "proven",
      "proof": "red",
      "control": "green",
      "control_exit": 0,
      "confirmation": "green",
      "exit": 1,
      "confirmation_exit": 0,
      "removal": "evidence-removed",
      "removal_exit": null,
      "reason": "the REMOVAL was NOT TAKEN: this criterion's own evidence (its command or its `Expect:` regex) names `enrichment_gap`, which the strip itself deleted from the tree, so the command was going to come back red whatever the behaviour does — that red would be a fact about the strip, not about the criterion. This transition has nothing to say here; it speaks only about a criterion whose evidence outlives the work it checks",
      "stderr_excerpt": ""
    },
    {
      "id": "AC-3",
      "command": "cargo test -p mustard-rt --lib commands::event::enrichment_gap::tests::no_census_means_an_empty_gap -- --exact 2>&1 | grep -q \"test result: ok. 1 passed\"",
      "expect": null,
      "control_command": "cargo test -p mustard-rt --lib scan_patterns 2>&1 | grep -q \"test result: ok\"",
      "verdict": "proven",
      "proof": "red",
      "control": "green",
      "control_exit": 0,
      "confirmation": "green",
      "exit": 1,
      "confirmation_exit": 0,
      "removal": "evidence-removed",
      "removal_exit": null,
      "reason": "the REMOVAL was NOT TAKEN: this criterion's own evidence (its command or its `Expect:` regex) names `enrichment_gap`, which the strip itself deleted from the tree, so the command was going to come back red whatever the behaviour does — that red would be a fact about the strip, not about the criterion. This transition has nothing to say here; it speaks only about a criterion whose evidence outlives the work it checks",
      "stderr_excerpt": ""
    },
    {
      "id": "AC-4",
      "command": "cargo test -p mustard-rt --test plugin_prose_matches_shipped_behaviour the_router_prose_names_the_signal_the_gate_emits -- --exact 2>&1 | grep -q \"test result: ok. 1 passed\"",
      "expect": null,
      "control_command": "cargo test -p mustard-rt --test command_frontmatter 2>&1 | grep -q \"test result: ok\"",
      "verdict": "proven",
      "proof": "red",
      "control": "green",
      "control_exit": 0,
      "confirmation": "green",
      "exit": 1,
      "confirmation_exit": 0,
      "removal": "evidence-removed",
      "removal_exit": null,
      "reason": "the REMOVAL was NOT TAKEN: this criterion's own evidence (its command or its `Expect:` regex) names `the_router_prose_names_the_signal_the_gate_emits`, which the strip itself deleted from the tree, so the command was going to come back red whatever the behaviour does — that red would be a fact about the strip, not about the criterion. This transition has nothing to say here; it speaks only about a criterion whose evidence outlives the work it checks",
      "stderr_excerpt": ""
    },
    {
      "id": "AC-5",
      "command": "cargo build --workspace",
      "expect": null,
      "control_command": null,
      "verdict": "exempt",
      "proof": "not-attempted",
      "control": "not-declared",
      "control_exit": null,
      "confirmation": "not-taken",
      "exit": null,
      "confirmation_exit": null,
      "removal": "not-taken",
      "removal_exit": null,
      "reason": "exempt by position: the trailing criterion is the build-green safety net, green before the work by design",
      "stderr_excerpt": ""
    }
  ],
  "amendments": [],
  "additions": []
}
