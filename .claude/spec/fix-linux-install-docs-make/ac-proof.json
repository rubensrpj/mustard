{
  "spec": "fix-linux-install-docs-make",
  "criteria": [
    {
      "id": "AC-1",
      "command": "bash -c \"tr -d '\\r' < packaging/installer/install.sh | sh -s -- --dry-run\"",
      "expect": null,
      "control_command": "bash -c \"tr -d '\\r' < packaging/installer/install.sh | sh -n\"",
      "verdict": "proven",
      "proof": "red",
      "control": "green",
      "control_exit": 0,
      "confirmation": "green",
      "exit": 1,
      "confirmation_exit": 0,
      "removal": "evidence-removed",
      "removal_exit": null,
      "reason": "the REMOVAL was NOT TAKEN: this criterion's own evidence (its command or its `Expect:` regex) names `--dry-run`, which the strip itself deleted from the tree, so the command was going to come back red whatever the behaviour does — that red would be a fact about the strip, not about the criterion. This transition has nothing to say here; it speaks only about a criterion whose evidence outlives the work it checks",
      "stderr_excerpt": ""
    },
    {
      "id": "AC-2",
      "command": "bash -c \"grep -q 'releases/latest/download/install' packaging/installer/TUTORIAL-LINUX.md && grep -q 'releases/latest/download/install' packaging/installer/RELEASE-BODY.md && grep -q 'releases/latest/download/install' README.md && grep -q 'releases/latest/download/install' README.en.md && grep -q 'plugin marketplace add rubensrpj/mustard' packaging/installer/TUTORIAL-LINUX.md && grep -q 'plugin marketplace add rubensrpj/mustard' README.md && grep -q 'plugin marketplace add rubensrpj/mustard' README.en.md\"",
      "expect": null,
      "control_command": "bash -c \"tr -d '\\r' < packaging/installer/install.sh | sh -n\"",
      "verdict": "proven",
      "proof": "red",
      "control": "green",
      "control_exit": 0,
      "confirmation": "green",
      "exit": 1,
      "confirmation_exit": 0,
      "removal": "evidence-removed",
      "removal_exit": null,
      "reason": "the REMOVAL was NOT TAKEN: this criterion's own evidence (its command or its `Expect:` regex) names `rubensrpj/mustard`, which the strip itself deleted from the tree, so the command was going to come back red whatever the behaviour does — that red would be a fact about the strip, not about the criterion. This transition has nothing to say here; it speaks only about a criterion whose evidence outlives the work it checks",
      "stderr_excerpt": ""
    },
    {
      "id": "AC-3",
      "command": "cargo build --workspace",
      "expect": null,
      "control_command": null,
      "verdict": "exempt",
      "proof": "not-attempted",
      "control": "not-declared",
      "control_exit": null,
      "confirmation": "not-taken",
      "exit": null,
      "confirmation_exit": null,
      "removal": "not-taken",
      "removal_exit": null,
      "reason": "exempt by position: the trailing criterion is the build-green safety net, green before the work by design",
      "stderr_excerpt": ""
    }
  ],
  "amendments": [],
  "additions": []
}
